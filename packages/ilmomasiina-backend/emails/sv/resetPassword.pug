extends ../layout.pug

block mainContent
  div.content-block
    p.bodyText Ditt lösenord till TF evenemang har återställts.
    ul
      li #[strong E-post:] #{email}
      li #[strong Nytt lösenord:] #{password}
  p.bodyText Du kan logga in på #[a(href=siteUrl) #{siteUrl}].
